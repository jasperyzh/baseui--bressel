# WordPress GraphQL Endpoint (live DO server)
WPGRAPHQL_ENDPOINT=http://104.248.157.67/graphql

# HTTP Basic Auth credentials (for protected WP instances)
WP_AUTH_USER=your_wp_username
WP_AUTH_PASSWORD=your_wp_password

# Optional: Override to use local Docker instead
# WPGRAPHQL_ENDPOINT=http://localhost:8080/graphql
# Then comment out the auth lines above
